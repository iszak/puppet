---
:backends:
    - yaml

:yaml:
    :datadir: "/etc/puppet/environments/%{::environment}/hieradata/"

:hierarchy:
    - private
    - users/vagrant
    - defaults

:logger: console

:merge_behavior: deeper
