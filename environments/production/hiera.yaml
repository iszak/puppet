---
:backends:
    - yaml

:yaml:
    :datadir: "/etc/puppet/environments/%{::environment}/hieradata/"

:hierarchy:
    - private
    - roles/%{::role}
    - users/iszak
    - defaults

:logger: console

:merge_behavior: deeper
