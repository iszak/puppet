---
:backends:
    - yaml

:yaml:
    :datadir: "/etc/puppet/hieradata/%{::environment}/"

:hierarchy:
    - private
    - roles/%{::role}
    - users/iszak
    - defaults

:logger: console

:merge_behavior: deeper
